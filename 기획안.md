# 쇼크트리 훈프로 쿠팡 광고 분석기 - 모바일 앱 기획안 v1.0

> 작성일: 2026-04-14 | React Native + Expo | iOS & Android 무료 앱

---

## 1. 프로젝트 개요

### 1-1. 현재 상태
- **Streamlit 기반 웹앱** (`1.md`)으로 운영 중
- 엑셀/CSV 파일을 수동 업로드하여 쿠팡 광고 성과 분석
- 비밀번호 기반 접근 제한 (202603)
- 지면별 분석, 옵션별 분석, 키워드 제외 제안, CTR/CVR/ROAS 분석 제공

### 1-2. 개발 목표
- Streamlit 웹앱 → **React Native + Expo 모바일 앱** (iOS/Android)
- 쿠팡 API 연동을 통한 **자동 데이터 수집** (가능 범위 내)
- 훈프로 정밀 분석 섹터 **고도화** (구체적이고 차별화된 분석)
- **완전 무료** (광고 없음, 유료 기능 없음)
- **Allra(올라) 선정산 제휴 배너** 삽입

---

## 2. 쿠팡 판매자 API 조사 결과

### 2-1. API 현황

| 구분 | 상태 | 설명 |
|------|------|------|
| 상품 관리 API | **제공** | 상품 조회/등록/수정/삭제 |
| 주문 관리 API | **제공** | 주문 조회/발송 처리 |
| 정산 API | **제공** | 정산 내역 조회 |
| **광고 성과 API** | **미제공** | 공개 문서 없음 |

### 2-2. API 인증 방식
- **HMAC-SHA256** 서명 기반 인증
- 필요 정보: `Access Key`, `Secret Key`, `Vendor ID`
- WING 판매자 포털(wing.coupang.com)에서 발급
- Rate Limit: 초당 10회

### 2-3. 결론 및 대응 전략

**쿠팡 광고 성과 데이터를 API로 자동 수집하는 것은 현재 불가능합니다.**

따라서 아래 **하이브리드 전략**을 채택합니다:

| 기능 | 방식 | 설명 |
|------|------|------|
| 광고 보고서 분석 | **엑셀 업로드 (유지)** | WING에서 다운로드한 보고서 파일 분석 |
| 매출/정산 데이터 | **API 연동 (신규)** | 쿠팡 API로 주문/정산 데이터 자동 조회 |
| 상품 정보 | **API 연동 (신규)** | 판매가, 수수료 등 자동 불러오기 |

> **향후 쿠팡이 광고 API를 공개하면 즉시 연동 가능하도록 설계합니다.**

---

## 3. 앱 구조 & 화면 설계

### 3-1. 탭 구조 (Bottom Tab Navigation)

```
[홈] [WING] [분석] [설정]
```

### 3-2. 화면별 상세

#### Tab 1: 홈 (Home)
- 앱 로고 & 타이틀: "쇼크트리 훈프로 쿠팡 광고 분석기"
- **최근 분석 히스토리** (로컬 저장, 최근 5건)
- **Allra 선정산 배너** (하단 고정)
  - Allra 로고 이미지 (`alla.png`) 표시
  - 텍스트: "선정산이 필요하신가요? Allra에서 빠르게 정산받으세요"
  - 클릭 시 → `https://m.site.naver.com/1Qv1P` 외부 브라우저 오픈
- **빠른 분석 시작 버튼** → 분석 탭으로 이동

#### Tab 2: WING 보고서 (신규 - 핵심 기능)
- **앱 내 WebView로 WING 모바일 웹 표시**
  - `wing.coupang.com` 모바일 웹 버전 로드 (User-Agent 변경 불필요)
  - WING 로그인 → 광고관리 → 보고서 다운로드까지 앱 안에서 완결
- **모바일 화면 최적화 (다운로드 버튼 접근성 개선)**
  - 문제: WING 모바일 웹에서 다운로드 버튼이 오른쪽 끝에 있어 접근 어려움
  - 해결 1: WebView viewport를 넓게 설정 (`width=1200`) → 페이지 전체가 축소되어 보임
  - 해결 2: **플로팅 "다운로드" 버튼** 제공 → 누르면 JS로 다운로드 버튼 영역 자동 스크롤
  - 해결 3: 페이지 로드 후 CSS 주입으로 테이블 글씨/패딩 축소하여 가로 공간 확보
  - 핀치 줌 지원 → 사용자가 필요시 확대/축소 가능
- **다운로드 자동 감지**
  - WebView의 `onFileDownload` / `onDownloadStart` 이벤트 감지
  - 엑셀/CSV 다운로드 시 자동으로 앱 내부 저장소에 저장
  - 다운로드 완료 → **"분석하기" 버튼 자동 팝업** 표시
  - 탭하면 즉시 분석 탭으로 이동하여 결과 표시
- **WING 로그인 세션 유지**
  - WebView 쿠키 유지 → 매번 로그인할 필요 없음
  - 설정에서 쿠키 초기화(로그아웃) 가능

#### Tab 3: 분석 (Analyze)
- **Step 1: 마진 설정**
  - 판매가, 최종원가, 입출고비, 수수료율 입력
  - 개당 예상 마진 실시간 계산 표시
- **Step 2: 데이터 입력**
  - **방법 A (자동)**: WING 탭에서 다운로드한 파일 자동 불러오기
  - **방법 B (수동)**: 기기 내 엑셀/CSV 파일 직접 선택 (Document Picker)
- **Step 3: 분석 결과**
  - 핵심 성과 지표 카드 (순이익, 광고비, ROAS, 판매수량)
  - 지면별 상세 분석 테이블
  - 옵션별 성과 분석 (효자/손실 옵션)
  - 제외 키워드 제안 (클립보드 복사)
  - **훈프로 정밀 분석** (고도화 버전 - 아래 섹션 4 참조)

#### Tab 4: 설정 (Settings)
- **WING 연동 관리**
  - WING 로그인 상태 표시
  - 쿠키 초기화 (로그아웃) 버튼
- **쿠팡 API 연동 (상품/주문용)**
  - Access Key / Secret Key / Vendor ID 입력란
  - 연결 테스트 버튼 & 연동 상태 표시
  - SecureStore 암호화 저장
- **마진 프리셋 저장**
  - 자주 사용하는 마진 설정을 저장/불러오기
- **앱 정보**
  - 버전, 개발자 정보, 훈프로 홈페이지 링크
- **Allra 선정산 안내** (배너 또는 링크)

---

## 4. 훈프로 정밀 분석 고도화

### 4-1. 현재 문제점
- CTR/CVR/ROAS 분석이 단순 임계값 기반 2분기 분류 (높다/낮다)
- 모든 상품에 동일한 메시지 → 실질적 도움이 안 됨
- 목표수익률 제안이 구간별 일률적 문구

### 4-2. 개선 방향: 데이터 기반 맞춤 분석

#### A. CTR (클릭률) 정밀 분석
```
분석 요소:
├── CTR 수치 구간 세분화 (0.3% 미만 / 0.3~0.5% / 0.5~1.0% / 1.0~2.0% / 2.0% 이상)
├── 지면별 CTR 비교 (검색 vs 카테고리 vs 추천 vs 기타)
│   └── 각 지면 평균 CTR과 비교하여 상대적 위치 분석
├── CTR 대비 노출수 관계 분석
│   └── 노출 많은데 CTR 낮음 → 썸네일 문제
│   └── 노출 적고 CTR 높음 → 입찰가 상향으로 노출 확대 권장
└── 구체적 액션 아이템
    ├── 썸네일 개선 포인트 (배경색, 텍스트 위치, 가격 강조 등)
    ├── 경쟁사 대비 차별화 포인트 제안
    └── 지면별 차별 전략 (검색 지면 CTR 낮으면 키워드 재검토)
```

#### B. CVR (구매전환율) 정밀 분석
```
분석 요소:
├── CVR 수치 구간 세분화 (1% 미만 / 1~3% / 3~5% / 5~10% / 10% 이상)
├── CVR vs CPC 효율 분석
│   └── CPC가 높은데 CVR 낮음 → 광고비 낭비, 키워드 정밀도 문제
│   └── CPC 낮고 CVR 높음 → 가성비 좋은 키워드, 예산 확대 권장
├── 판매수량 대비 클릭수 비율로 상세페이지 설득력 진단
├── 지면별 CVR 차이 분석
│   └── 검색 CVR > 카테고리 CVR → 키워드 타겟팅 정확도 양호
│   └── 카테고리 CVR > 검색 CVR → 검색 키워드 재검토 필요
└── 구체적 액션 아이템
    ├── 상세페이지 최적화 (상단 3초 후킹, 혜택 강조, 리뷰 관리)
    ├── 가격 경쟁력 점검 제안
    ├── 쿠폰/프로모션 활용 전략
    └── 키워드-상품 매칭도 점검
```

#### C. 목표수익률 최적화 가이드 (핵심 개선)
```
분석 로직:
├── 손익분기 ROAS 자동 계산
│   └── BEP ROAS = 판매가 / (판매가 - 원가 - 수수료 - 입출고비)
│   └── "귀하의 손익분기 ROAS는 XXX%입니다"
├── 현재 ROAS vs 손익분기 ROAS 비교
│   └── 현재가 BEP 이하 → 적자 구간, 얼마나 부족한지 수치로 표시
│   └── 현재가 BEP 이상 → 흑자 구간, 여유분 수치로 표시
├── 목표수익률(%) 역산 제안
│   └── 현재 CPC와 CVR 기반으로 적정 목표수익률 계산
│   └── "현재 CPC {X}원, CVR {Y}%일 때 목표수익률을 {Z}%로 설정하면"
│   └── "예상 광고비 대비 순이익이 {W}원이 됩니다"
├── 시나리오 비교 (3단계)
│   ├── 보수적: 목표수익률 +50%p → 예상 효과
│   ├── 균형형: 현재 유지 → 예상 효과
│   └── 공격적: 목표수익률 -30%p → 예상 효과
└── 지면별 차등 목표수익률 제안
    └── 검색 지면 ROAS 높으면 → 검색은 유지, 카테고리는 상향
```

#### D. 종합 진단 점수 (신규)
```
총점 100점 기준:
├── CTR 점수 (30점): 지면별 가중평균
├── CVR 점수 (30점): 업종 평균 대비 상대 평가
├── ROAS 효율 점수 (25점): 손익분기 대비 여유율
└── 키워드 효율 점수 (15점): 제외 대상 키워드 비율
→ S/A/B/C/D 등급 부여 + 우선 개선 영역 1가지 명시
```

---

## 5. Allra(올라) 선정산 제휴 연동

### 5-1. 배너 위치
1. **홈 탭 하단**: 고정 배너 (가장 눈에 잘 띄는 위치)
2. **분석 결과 하단**: 분석 완료 후 자연스러운 노출
3. **설정 탭**: "파트너 서비스" 섹션

### 5-2. 배너 디자인
```
┌─────────────────────────────────────────────┐
│  [Allra 로고]  선정산이 필요하신가요?         │
│               Allra에서 빠르게 정산받으세요 →  │
└─────────────────────────────────────────────┘
```
- 로고: `alla.png` 사용
- 클릭 시: `https://m.site.naver.com/1Qv1P` → `Linking.openURL()` 외부 브라우저 오픈
- 배경: 연한 블루 (#E8F4FD) + 둥근 모서리

---

## 6. 기술 아키텍처

### 6-1. 기술 스택
```
프레임워크:    React Native + Expo SDK 54
언어:         TypeScript (strict)
스타일링:     NativeWind (TailwindCSS)
상태관리:     Zustand
라우팅:       Expo Router (탭 기반)
파일처리:     expo-document-picker + xlsx 라이브러리
보안저장:     expo-secure-store (API 키 암호화 저장)
HTTP:         axios (HMAC 서명 생성 포함)
차트:         react-native-chart-kit 또는 victory-native
링크:         expo-linking (외부 URL 오픈)
```

### 6-2. 폴더 구조
```
app/
├── (tabs)/
│   ├── index.tsx          # 홈 탭
│   ├── wing.tsx           # WING WebView 탭 (핵심)
│   ├── analyze.tsx        # 분석 탭
│   └── settings.tsx       # 설정 탭
├── _layout.tsx            # Root Layout
├── result.tsx             # 분석 결과 화면
components/
├── AllraBanner.tsx         # Allra 제휴 배너
├── WingWebView.tsx         # WING 모바일웹 WebView (viewport 최적화)
├── MarginCalculator.tsx    # 마진 계산기
├── FileUploader.tsx        # 파일 업로드
├── KpiCards.tsx            # 핵심 지표 카드
├── AnalysisTable.tsx       # 분석 테이블
├── PrecisionAnalysis.tsx   # 정밀 분석 (CTR/CVR/ROAS)
├── ScoreCard.tsx           # 종합 진단 점수
└── KeywordExclude.tsx      # 제외 키워드
services/
├── coupangApi.ts           # 쿠팡 API 통신 (HMAC 인증)
├── excelParser.ts          # 엑셀 파싱 로직
└── analysisEngine.ts       # 분석 엔진 (CTR/CVR/ROAS 계산)
stores/
├── useSettingsStore.ts     # 설정 상태 (API 키, 마진 프리셋)
├── useAnalysisStore.ts     # 분석 데이터 상태
hooks/
├── useCoupangApi.ts        # API 호출 훅
├── useFileImport.ts        # 파일 임포트 훅
├── useWingDownload.ts      # WING 다운로드 감지 훅
constants/
├── analysisThresholds.ts   # CTR/CVR/ROAS 임계값 & 메시지
├── colors.ts               # 색상 상수
assets/
└── alla.png                # Allra 로고
```

### 6-3. 쿠팡 API 연동 설계

```typescript
// services/coupangApi.ts - HMAC 인증 흐름
import CryptoJS from 'crypto-js';

interface CoupangCredentials {
  accessKey: string;
  secretKey: string;
  vendorId: string;
}

// HMAC-SHA256 서명 생성
function generateHmacSignature(method: string, path: string, secretKey: string): string {
  const datetime = new Date().toISOString().replace(/[-:]/g, '').split('.')[0] + 'Z';
  const message = `${datetime}${method}${path}`;
  return CryptoJS.HmacSHA256(message, secretKey).toString(CryptoJS.enc.Hex);
}

// 사용 가능한 엔드포인트:
// - GET /v2/providers/seller_api/apis/api/v1/marketplace/seller-products → 상품 목록
// - GET /v2/providers/openapi/apis/api/v4/vendors/{vendorId}/returnShippingCenters → 정산
// 광고 API는 미제공 → 엑셀 업로드로 대체
```

---

## 7. 데이터 흐름

```
[사용자]
   │
   ├── 엑셀 업로드 ──→ expo-document-picker ──→ xlsx 파싱 ──→ analysisEngine
   │                                                              │
   ├── API 연동 설정 ──→ SecureStore 저장 ──→ coupangApi ──→ 상품/주문 데이터
   │                                                              │
   └── 마진 설정 입력 ──→ Zustand Store ─────────────────────────→ │
                                                                   ↓
                                                          [분석 결과 생성]
                                                              │
                                                 ┌────────────┼────────────┐
                                                 ↓            ↓            ↓
                                           [KPI 카드]  [정밀 분석]  [키워드 제안]
                                                              │
                                                         [종합 점수]
```

---

## 8. 개발 우선순위 & 단계

### Phase 1: 코어 기능 (MVP)
1. Expo 프로젝트 초기화 & 4탭 네비게이션 설정
2. **WING WebView 탭 구현** (모바일웹, viewport 최적화, 핀치 줌, 쿠키 유지)
3. **WebView 다운로드 감지 & 파일 자동 저장**
4. 마진 계산기 UI
5. 엑셀/CSV 파싱 엔진 (Python → TypeScript 이식)
6. 분석 결과 화면 (KPI 카드, 테이블)
7. Allra 배너 삽입

### Phase 2: 분석 고도화
8. CTR 정밀 분석 (5단계 구간 + 지면별 비교)
9. CVR 정밀 분석 (CPC 효율 분석 + 상세페이지 진단)
10. 목표수익률 역산 & 시나리오 비교
11. 종합 진단 점수 시스템

### Phase 3: API 연동 (상품/주문용)
12. 설정 화면 - API 키 입력 & 암호화 저장
13. HMAC 인증 모듈 구현
14. 상품 정보 자동 불러오기 (판매가 자동 입력)

### Phase 4: 마무리
15. 분석 히스토리 (AsyncStorage)
16. 마진 프리셋 저장/불러오기
17. 다크모드 지원
18. 앱 아이콘 & 스플래시 스크린
19. 빌드 & 배포 (EAS Build)

---

## 9. 쿠팡 API 테스트 시 필요 사항

앱 배포 전 API 연동 테스트를 위해 아래 정보가 필요합니다:

- **Access Key**: WING에서 발급 (WING → 판매자정보 → OPEN API)
- **Secret Key**: Access Key와 함께 발급
- **Vendor ID**: WING 판매자 ID

> 개발 진행 시 API 키를 요청드리겠습니다.

---

## 10. 배포 계획

| 항목 | 내용 |
|------|------|
| 앱 이름 | 쇼크트리 훈프로 쿠팡 광고 분석기 |
| 가격 | **무료** (광고 없음, 인앱결제 없음) |
| 수익 모델 | Allra 선정산 제휴 (CPA/CPS) |
| iOS | App Store (Apple Developer 계정 필요 - 연 $99) |
| Android | Google Play (개발자 계정 필요 - 1회 $25) |
| 빌드 | EAS Build (Expo) |

---

## 11. 주의사항 & 제약

1. **광고 API 미제공**: 쿠팡 광고 성과 데이터는 API로 불가 → 엑셀 업로드 유지 필수
2. **API 키 보안**: SecureStore 사용, 평문 저장 절대 금지
3. **Rate Limit**: 초당 10회 제한, 요청 큐 관리 필요
4. **파일 크기**: 모바일 환경 고려, 대용량 엑셀 파싱 시 메모리 관리
5. **오프라인**: 엑셀 분석은 오프라인에서도 가능하도록 설계
